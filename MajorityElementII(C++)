vector<int> majorityElement(vector<int>& nums) {
    int qualifier = nums.size()/3;
    map<int, int> m;
    vector<int> elements;

    for(auto n : nums){
        if(++m[n] == qualifier + 1){ 
            numbers.push_back(n);
        }
    }
    
    return elements;
}
